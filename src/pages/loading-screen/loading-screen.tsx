function LoadingScreen(): JSX.Element {
  return (
    <div style={{height: '100vh', justifyContent: 'center', alignItems: 'center'}} className="wrapper">
      <div className='loader'></div>
    </div>);
}

export default LoadingScreen;
